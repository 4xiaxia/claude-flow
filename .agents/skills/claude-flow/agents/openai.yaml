interface:
  display_name: "Claude Flow"
  short_description: "Repo-specific patterns and workflows for claude-flow"
  default_prompt: "Use the claude-flow repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true